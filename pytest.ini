[pytest]
testpaths = tests
norecursedirs = tests/legacy_diagnostics
markers =
    slow: long-running calibration, search, or high-scale numerical gate
    master_calibration: merge-blocking validated leadengine calibration invariants
